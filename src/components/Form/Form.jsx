import React from "react";

const Form = () => {
    return (
        <div>
            <form action="">
                <input type="text" placeholder="ingrese su nombre" />
                <br />
                <button>Enviar</button>
            </form>
        </div>
    );
};

export default Form;
